# BasicInteractionFlow

main: kit: flow: BasicInteractionFlow:
{
  entity: "interaction",
  kind: "basic",
  name: "BasicInteractionFlow",
  step: [
    {
      input: {
        ref: "interaction_ref01"
      }

      op: "create"
    }

    {
      op: "list",
      valid: [
        {
          apply: "ItemExists",
          def: {
            ref: "interaction_ref01"
          }
        }
      ]
    }
  ],
  "key$": "BasicInteractionFlow"
}